{
  "name": "sanctum",
  "version": "0.1.0",
  "private": true,
  "description": "The online home for creators and communities.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web",
    "dev:api": "npm run dev --workspace apps/api"
  }
} 